[package]
name = "lockpass-server-auth"
version = "0.1.0"
description = "LockPass Sync authentication domain services"
authors = ["LockPass Next"]
license = ""
repository = ""
edition = "2021"
rust-version = "1.82"

[dependencies]
argon2 = "0.5"
hex = "0.4"
rand_core = { version = "0.6", features = ["getrandom"] }
sha2 = "0.10"
thiserror = "2"
uuid = { version = "1", features = ["v4"] }
