name: lockpass
services:
  server:
    image: ftyszyx752/lockpass:latest
    env_file:
      - .env
    ports:
      - "${SERVER_PORT:-8080}:8080"
    networks:
      - 1panel-network
    restart: unless-stopped
networks:
  1panel-network:
    external: true
